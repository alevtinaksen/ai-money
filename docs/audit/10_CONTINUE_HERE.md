# Контекст для продолжения работы

Дата: 26.09.2026. Текущая стадия: аудит и планирование завершены в доступных локальных границах; **реализация не начиналась**. Последнее ограничение пользователя: исходный проект не изменять, только исследовать и документировать. Новые сообщения добавили требования к будущему Antigravity pack и банковскому импорту, не разрешили сейчас исправлять код.

## Что сохранено

00 — основной вывод и границы;01 — доказанные дефекты и риски;02 — A0–A10 плюс I0–I5;03 — отказоустойчивость/ISO/RFC;04 — реестр внешних оснований;05 — передача Mac/Gemini;06 — воспроизведение;07 — требования quality pack;08 — единая гипотеза импорта;09 — опыт приложений/банковские источники; evidence — реальные логи/диагностические scripts, baseline и integrity.

Исходник: пользовательский `ai-money-clean.zip`. Его hash и список67 файлов находятся в evidence/baseline.json. Исходный архив и все67 файлов проверены неизменными. В пакет отчёта исходники не включены. Для нового аудита распаковать именно соответствующий hash или переснять baseline и перепроверить применимость находок.

## Самые важные результаты

Auth bypass сохраняется при DEBUG=False. PUT может менять чужой account. Stale balance update теряет расход. Seed ломает второго пользователя и восстанавливает удалённое. Parser неверно трактует обычные денежные форматы. Frontend маскирует network/HTTP failures и перекрывает свежий server state. Выбор фото создаёт константную сумму. .env template API key name не совпадает с кодом; локальный UI может уйти на внешний endpoint. Полные доказательства01, не исправлять на основании одного этого списка.

Existing tests7 PASS и frontend build PASS не закрывают эти проблемы. npm audit0, pip-audit0 для отдельно разрешённых50 dependencies; Mac/Python3.12/PostgreSQL/Telegram live отсутствовали. Формальный ISO PASS не выдавался.

## Дополнительные запросы пользователя

1. Будущий архив должен помогать Алине самостоятельно настроить Antigravity/MCP/roles/hooks/project-memory на Mac, с backup/rollback/doctor и проверкой реальной активации.
2. Включить исполняемые тестовые и deploy gates, отдельное ревью, разумные подсказки рефакторинга; общие практики для нашей системы перенести отдельной проверяемой задачей.
3. Изучить аналоги и банковские файлы; гипотеза: выписка→draft→review/comment/exclude/transfer/dedup→confirm→journal. До подтверждения баланс не меняется. Формат одного банка подтвердить образцом, не обещать универсальный XML и свободный banking API.

## Следующий шаг после разрешения на реализацию

Начать A0/A1 на изолированной ветке: backup/restore, явный local API endpoint, убрать липовый чек/demo из реальных путей, закрыть auth и ownership с негативными тестами. Не начинать с косметического распила файлов или нового banking API. Для новой функции I0 можно исследовать обезличенный образец параллельно; I2 apply требует money/dedup/baseline gates.

Не просить доступ к настоящему банковскому паролю или пересылку токенов. Не устанавливать настройки в текущем Windows окружении вместо Mac Алины. Не выдавать наличие config за работающий hook/MCP. Не отправлять материалы Алине без отдельной команды; пакет пока подготовлен для Влада.
