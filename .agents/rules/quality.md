---
trigger: always_on
description: Project evidence, feature ownership, safe refactoring and completion checks.
---

Follow the repository AGENTS.md and docs/QUALITY.md. Before changing behavior identify regression cases; after changing it run real checks. Keep clear feature boundaries and review oversized modules, never split merely to satisfy line count. Report observed results separately from unverified Mac/IDE/Telegram behavior. Do not silently skip failing or missing checks.
